{% skip() if not hconfig.vless_enable %}
"type": "vless",
"users": [
  {% for user in users %}
  {"name": "{{ user.uuid }}",    "uuid": "{{ user.uuid }}",    "flow": "{{ hconfig.vless_flow }}"},
  {% endfor %}
]
