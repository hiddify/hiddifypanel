{% if hconfig.mux_enable %}
,"multiplex": {
  "enabled": true,
  "padding": {% if hconfig.mux_padding_enable %}true{% else %}false{% endif %},
  "brutal": {
    "enabled": {% if hconfig.mux_brutal_enable %}true{% else %}false{% endif %},
    "up_mbps": {{ hconfig.mux_brutal_up_mbps }},
    "down_mbps": {{ hconfig.mux_brutal_down_mbps }}
  }
}
{% endif %}
