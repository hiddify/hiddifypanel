{% skip() if not hconfig.naive_enable %}
"type": "naive",
"server": "{{ proxy.server }}",
"server_port": {{ proxy.port }},
"username": "{{ user.uuid }}",
"password": "h",
"extra_headers":{
    "X-API-Key":"{{proxy.path}}"
}