{% skip() if not hconfig.mieru_enable %}
"type": "mieru",
"server": "{{ domain.server }}",
"multiplexing": "{{ hconfig.mieru_multiplexing }}",
"handshake_mode": "{{ hconfig.mieru_handshake }}",
"username": "{{ user.uuid }}",
"password": "h",
"portBindings": [
  {% for protocol,ports in [("TCP",proxy.tcp_port_ranges),("UDP",proxy.udp_port_ranges)] %}
  {% for entry in ports%}
  {
    "protocol": "{{protocol}}",
    {% if entry is number %}
    "port": {{ entry }}
    {% else %}
    "portRange": "{{ entry }}"
    {% endif %}
  },
  {% endfor %}
  {% endfor %}
  
],
