# Flask panel URL for Vite dev proxy (bootstrap + API).
VITE_API_TARGET=http://127.0.0.1:9000
VITE_DEV_PORT=5173

# Optional override — normally proxy_path is discovered automatically via /__admin_v2_bootstrap
# VITE_API_BASE=/{your-proxy-path}/api/v2/admin/
