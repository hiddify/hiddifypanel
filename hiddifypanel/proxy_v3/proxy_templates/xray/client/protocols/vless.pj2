"protocol": "vless",
"settings": {
  "vnext": [{
    "address": "{{ ctx.proxy.server }}",
    "port": {{ ctx.proxy.port }},
    "users": [{
      "id": "{{ ctx.user.uuid }}",
      "encryption": "{{ctx.hconfig.vless_encryption}}",
      "flow": "{{ ctx.hconfig.vless_flow }}",
      "level": 8
    }]
  }]
}
