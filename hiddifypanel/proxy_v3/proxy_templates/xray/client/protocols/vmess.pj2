"protocol": "vmess",
"settings": {
  "vnext": [{
    "address": "{{ ctx.proxy.server }}",
    "port": {{ ctx.proxy.tcp_port }},
    "users": [{
      "id": "{{ ctx.user.uuid }}",
      "security": "auto",
      "level": 8
    }]
  }]
}
