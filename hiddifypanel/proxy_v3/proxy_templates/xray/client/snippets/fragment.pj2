{% if (not ctx.proxy.is_reality) and ctx.proxy.uses_tls and ctx.hconfig.tls_fragment_enable %}
,"sockopt": {
  "dialerProxy": "fragment",
  "tcpKeepAliveIdle": 100,
  "tcpNoDelay": true
}
{% endif %}
