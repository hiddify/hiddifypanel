"type": "naive",
"server": "{{ proxy.server }}",
"server_port": {{ proxy.port }},
"username": "{{ ctx.user.uuid }}",
"password": "{{ proxy.path }}",
"extra_headers":{
    "X-API-Key":"{{proxy.path}}"
},
"quic": {{(ctx.proxy.tls_layer in ["quic","quic_tls"])|jsbool}},