"listen": "{{ ('::' if not hconfig.only_ipv4 else '0.0.0.0') if proxy.public_access else '127.0.0.1' }}",
"listen_port": {{proxy.port}},
"tag": "{{proxy.tag}}",
{% if not proxy.public_access %}
"proxy_protocol": true,
{% endif %}
