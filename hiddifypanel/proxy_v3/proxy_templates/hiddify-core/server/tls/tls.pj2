{% set domain = domain or ctx.domain %}
"tls": {
  "enabled": true,
  "server_name": "{{ domain.sni }}",
  "alpn": {{ tls_layer.alpns() }},

  {% if domain.cert.cert_lines %}
  "certificate": {{ domain.cert.cert_lines | tojson }},
  "key": {{ domain.cert.key_lines | tojson }},
  {% endif %}

},
