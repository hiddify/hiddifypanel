{% skip("tcp_enable = " ~ hconfig.tcp_enable) if not hconfig.tcp_enable %}
"transport": {
    "type": "http",
    "method": "GET",
    "path": "/{{ proxy.path }}",
    "idle_timeout": "15s",
    "ping_timeout": "15s"
},