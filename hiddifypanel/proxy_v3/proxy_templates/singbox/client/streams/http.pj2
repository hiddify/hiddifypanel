{{ skip("tcp_enable = " ~ ctx.hconfig.tcp_enable) if not ctx.hconfig.tcp_enable }}
"network": "tcp",
"transport": {
  "type": "http",
  "path": "/{{ proxy.path }}",
  "host": ["{{ proxy.domain.host }}"]
},
